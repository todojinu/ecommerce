# configuration server 정보 추가
spring:
  cloud:
    config:
      uri: http://127.0.0.1:8888
      #name: ecommerce  # ecommerce.yml
      name: config-service  # application.yml
  # profile 설정 추가
#  profiles:
#    active: dev  # profile이 dev인 파일을 읽음
